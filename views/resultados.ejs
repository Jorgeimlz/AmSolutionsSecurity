<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Resultados</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="/assets/styles/styles.css">
</head>
<body style="background-color: #000000; color: #ffffff;">
    <div class="container mt-5">
        <h1 class="text-center mb-4">Resultados para <%= dominio %></h1>
        <ul class="list-group">
            <% subdominios.forEach(function(subdominio) { %>
                <li class="list-group-item d-flex justify-content-between align-items-center" style="background-color: #000000; color: #ffffff;">
                    <span><%= subdominio.subdomain %> - IP: <%= subdominio.ip %></span>
                    <button class="btn btn-primary risk" data-risk="<%= subdominio.riesgo %>" data-dominio="<%= dominio %>">Riesgo: <%= subdominio.riesgo %></button>
                </li>
            <% }) %>
        </ul>
        <div class="text-center mt-4">
            <a href="/" class="btn btn-primary">Volver a la Búsqueda</a>
            <a href="/subdominios/reporte?dominio=<%= dominio %>" class="btn btn-secondary">Descargar Reporte PDF</a>
        </div>
    </div>

    <script src="/assets/js/scripts.js"></script>
</body>
</html>
