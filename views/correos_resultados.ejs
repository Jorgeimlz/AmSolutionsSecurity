<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Resultados de Correos Electrónicos</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="/assets/styles/styles.css">
</head>
<body style="background-color: #000000; color: #ffffff;">
    <div class="container mt-5">
        <h1 class="text-center mb-4">Correos Electrónicos para <%= dominio %></h1>
        <ul class="list-group">
            <% correos.forEach(function(correo) { %>
                <li class="list-group-item d-flex justify-content-between align-items-center" style="background-color: #000000; color: #ffffff;">
                    <span><%= correo.value %></span>
                    <button class="btn btn-primary risk" data-risk="<%= correo.riesgo %>" data-dominio="<%= dominio %>">Riesgo: <%= correo.riesgo %></button>
                </li>
            <% }) %>
        </ul>
        <div class="text-center mt-4">
            <a href="/index" class="btn btn-primary">Volver a la Búsqueda</a>
        </div>
    </div>

    <script src="/assets/js/scripts.js"></script>
</body>
</html>
