<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Resultados de Búsqueda</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="/assets/styles/styles.css">
</head>
<body style="background-color: #000000; color: #ffffff;">
    <div class="container mt-5">
        <h1 class="text-center mb-4">Resultados de Búsqueda para <%= query %></h1>
        <ul class="list-group">
            <% if (resultados && resultados.length > 0) { %>
                <% resultados.forEach(function(resultado) { %>
                    <li class="list-group-item d-flex justify-content-between align-items-center" style="background-color: #000000; color: #ffffff;">
                        <span><%= resultado.title %> - <a href="<%= resultado.link %>" class="text-info"><%= resultado.link %></a></span>
                    </li>
                <% }) %>
            <% } else { %>
                <li class="list-group-item" style="background-color: #000000; color: #ffffff;">
                    No se encontraron resultados.
                </li>
            <% } %>
        </ul>
        <div class="text-center mt-4">
            <a href="/index" class="btn btn-primary">Volver a la Búsqueda</a>
        </div>
    </div>
</body>
</html>

